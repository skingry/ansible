---
- name: plex-cleaner - pull and configure
  docker_container:
    name: plex-cleaner
    image: nitrikx/plex-cleaner
    pull: true
    env:
      EXECUTION_CRON_EXPRESSION: "22 */2 * * *"
    volumes:
      - /opt/config/plex-cleaner:/config:rw
      - /data/shares/Media:/media:rw
    restart_policy: always
